import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#01579b',
    },
    secondary: {
      main: '#fdd835',
    },
    background: {
      default: '#fafafa',
    },
  },
});

export default theme;
