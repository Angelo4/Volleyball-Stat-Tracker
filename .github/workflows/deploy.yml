name: Deployment

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: volleyball-stat-tracker-app

    steps:
      - uses: actions/checkout@v3
      - run: ls
      - run: echo deployingTest
